# Rust
src-tauri/target/
src-tauri/Cargo.lock
**/*.rs.bk

# Node
node_modules/

# Build artifacts
src-tauri/gen/
*.log
*.tmp

# 机器本地构建配置（中文路径下的 target-dir 修正，不入库）
src-tauri/.cargo/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*~

# EPUB / Markdown 个体测试样本（不随仓库发布）
*.epub
*.exe
*.zip
*.msi

# 测试用的 MD 文件（该文件在内存提供 MD 渲染测试，不随仓库发布）
test-reader.md

# 本地分析 / 草稿（不随仓库发布）
CODE-SLIM-REPORT.md
/reports/
/scratch/

# — npm lockfile 保留以保证复现 —
# （package-lock.json **不要**忽略，删掉下行注释）
